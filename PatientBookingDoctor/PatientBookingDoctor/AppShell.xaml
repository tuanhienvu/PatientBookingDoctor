<?xml version="1.0" encoding="UTF-8"?>

<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:PatientBookingDoctor.Views"
       x:Class="PatientBookingDoctor.AppShell">
    <Shell.Resources>
        <!-- Style Shell elements -->
        <Style x:Key="BaseStyle"
               TargetType="Element">
            <Setter Property="Shell.BackgroundColor"
                    Value="{StaticResource AppPrimaryColor}" />
            <Setter Property="Shell.ForegroundColor"
                    Value="{StaticResource SecondaryColor}" />
            <Setter Property="Shell.TitleColor"
                    Value="{StaticResource SecondaryColor}" />
            <Setter Property="Shell.TabBarUnselectedColor"
                    Value="#95FFFFFF"/>
        </Style>
        <Style TargetType="TabBar"
               BasedOn="{StaticResource BaseStyle}" />
    </Shell.Resources>
    <!-- 
        When the Flyout is visible this defines the content to display in the flyout.
        FlyoutDisplayOptions="AsMultipleItems" will create a separate flyout item for each child element    
        https://docs.microsoft.com/dotnet/api/xamarin.forms.shellgroupitem.flyoutdisplayoptions?view=xamarin-forms
    -->
    <!-- <FlyoutItem Title="Home" Icon="icon_feed.png"> -->
    <!--     <ShellContent Route="HomePage" ContentTemplate="{DataTemplate views:HomePage}" /> -->
    <!-- </FlyoutItem> -->
    <!-- -->
    <!-- <FlyoutItem Title="Hotline" Icon="icon_about.png"> -->
    <!-- <ShellContent Route="HotlinePage" ContentTemplate="{DataTemplate views:HotlinePage}" /> -->
    <!-- </FlyoutItem> -->
    <!-- -->
    <!-- <FlyoutItem Title="About" Icon="icon_about.png"> -->
    <!--     <ShellContent Route="AboutPage" ContentTemplate="{DataTemplate views:AboutPage}" /> -->
    <!-- </FlyoutItem> -->

    <!-- When the Flyout is visible this will be a menu item you can tie a click behavior to  -->
    <MenuItem Text="Logout" StyleClass="MenuItemsLayoutStyle" Clicked="OnMenuItemClicked">
    </MenuItem>
    <!-- Display a bottom tab bar containing three tabs -->
    <TabBar x:Name="main_nav" Route="main">
        <Tab x:Name="nav_hotline" 
             Icon="phone.png"
             Title="Hotline">
            <ShellContent ContentTemplate="{DataTemplate views:HotlinePage}" />
        </Tab>
        <Tab x:Name="nav_home" 
             Icon="home_white.png"
             Title="Home">
            <ShellContent ContentTemplate="{DataTemplate views:HomePage}" />
        </Tab>
        <Tab x:Name="nav_settings"
             Icon="menu_white.png"
             Title="Menu">
            <ShellContent ContentTemplate="{DataTemplate views:MenuPage}" />
        </Tab>
    </TabBar>
</Shell>